List of removed CSS rules and their Bootstrap replacements:

1. Removed `display: flex`, `flex-wrap`, and `gap` for `.review-gallery`.
   Replaced with Bootstrap Grid system: `.row`, `.g-3`, and `.col-12 .col-md-4`.
   
2. Removed `display: grid` and `grid-template-columns` for `.booking-form fieldset`.
   Replaced with Bootstrap Grid system: `.row`, `.g-3`, `.col-md-6`, and `.col-12`.

3. Removed `float: left` and `clear: both` from `.float-example` and `.clear-float`.
   Replaced with Bootstrap utilities: `.float-start`, `.clearfix`, and `.me-3`.

4. Removed custom media queries `@media (max-width: 700px)`.
   Replaced with Bootstrap's responsive breakpoints (`.col-md-6`, `.d-none`, `.d-md-block`).

5. Removed inline styles (`style="justify-content: center;"`).
   Replaced with Bootstrap utility classes (`.text-center`, `.justify-content-center`).

6. Removed manual styling for buttons (background, hover, padding).
   Replaced with Bootstrap button classes: `.btn`, `.btn-primary`, `.btn-lg`, `.btn-outline-secondary`.